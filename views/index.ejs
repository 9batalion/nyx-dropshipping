<div class="hero-banner">
    <h1>Witamy w naszym sklepie!</h1>
    <p>Najgorętsze produkty w doskonałych cenach</p>
    <a class="btn btn-light btn-lg" href="#products">Kup teraz</a>
</div>
    <div class="col-12">
        <h1 class="display-4">Najgorętsze trendy tylko u nas!</h1>
        <p class="lead">Odkryj unikalne produkty, które robią furorę w sieci. Najwyższa jakość i szybka dostawa, na które możesz liczyć.</p>
        <a href="#products" class="btn btn-light btn-lg mt-4">Zobacz produkty</a>
    </div>
</div>

<div class="row mt-4">
    <% products.forEach(product => { %>
        <div class="col-md-6 col-lg-3 mb-4">
            <div class="card product-card h-100">
                <img src="<%= product.image %>" class="card-img-top" alt="<%= product.name %>">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><%= product.name %></h5>
                    <p class="card-text flex-grow-1"><%= product.description %></p>
                    <div class="mt-auto">
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="h5 text-primary"><%= product.price.toFixed(2) %> zł</span>
                            <a href="/product/<%= product.id %>" class="btn btn-primary">Szczegóły</a>
                        </div>
                        <button class="btn btn-success w-100 mt-2 add-to-cart">Dodaj do koszyka</button>
                    </div>
                </div>
            </div>
        </div>
    <% }); %>
</div>